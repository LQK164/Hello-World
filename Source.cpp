#include <iostream>
#include <string>
#include <sstream>

using namespace std;
struct Node
{
	float Data;
	struct Node* pNext;
};
typedef struct Node NODE;
struct Stack
{
	NODE* Top = NULL;
};

			break;
		default:
			istringstream in(s);
			in >> after;
			Push(oprnd, after);
		}
	}
	return res;
}